#### 再做显示大模型输出内容时，因为考虑输出格式存在多样式，约定使用Markdown形式进行展示，其中一部分关键卡片数据，需要使用marddown的自定义block进行引入，包括分析结果的展示。尤其是分析结果不可能重新写一个新的组件，需要服用原有的分析组件提供的SDK进行展示。但markdown自定义block中的组件直接是不支持事件冒出的。后面想到用web-componet包裹实现，保证引入的自定义组件能正常出发操作响应。但引入了web-componet又会带来样式隔离问题，SDK本身依赖项目中的一些全局样式，因为web-componet的隔离特性，导致样式丢失，那么就需要再使用的sdk的地方，把相关的样式重新写一遍。？

这里有没有更好的方式呢？
